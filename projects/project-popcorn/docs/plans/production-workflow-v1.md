# Cross-Domain Radar 운영 파이프라인 설계 v1

> **버전:** v1
> **작성일시:** 2026-01-19 10:00
> **상태:** 설계 검토 중

---

## 1. 설계 원칙

### 1.1 핵심 요구사항
| # | 요구사항 | 해결 방안 |
|---|----------|-----------|
| 1 | 데이터 흐름 연속성 | 일배치 수집 파이프라인 |
| 2 | 빠른 운영 진입 + 피드백 | Teams 웹훅 알림 |
| 3 | 부처별 동적 임계값 | 부처 프로필 기반 임계값 |
| 4 | 핵심 법안 누락 방지 | 관심 키워드 가산점 로직 |
| 5 | 서버 환경 | 로컬 PC (24시간 가동) |
| 6 | 실행 주기 | 일배치 (1일 1회) |

### 1.2 아키텍처 개요

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    Production Pipeline Architecture                      │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌────────┐│
│  │ 1.수집  │───▶│ 2.임베딩│───▶│ 3.스코어│───▶│ 4.알림  │───▶│5.리포트││
│  │ Ingest  │    │ Embed   │    │ Score   │    │ Alert   │    │ Report ││
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘    └────────┘│
│       │              │              │              │              │     │
│       ▼              ▼              ▼              ▼              ▼     │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌────────┐│
│  │국회 API │    │ OpenAI  │    │동적임계값│    │ Teams   │    │ HTML   ││
│  │신규 법안│    │ + 캐시  │    │+키워드  │    │ Webhook │    │ Daily  ││
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘    └────────┘│
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────────┐│
│  │                     Scheduler (launchd / cron)                      ││
│  │                     매일 09:00 실행                                  ││
│  └─────────────────────────────────────────────────────────────────────┘│
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 상세 설계

### 2.1 수집 파이프라인 (Ingest)

```
국회 API ──▶ 신규 법안 수집 ──▶ bills_daily_{date}.json
             (어제 이후 제안)
```

**데이터 소스:**
- 열린국회정보 의안정보 API
- 공공데이터포털 BillInfoService

**수집 로직:**
```python
def fetch_new_bills(since_date: str) -> list[dict]:
    """
    since_date 이후 제안된 법안 수집
    - propose_dt >= since_date 필터링
    - 중복 제거 (bill_id 기준)
    """
```

**저장 구조:**
```
data/
├── daily/
│   ├── 2026-01-19_bills.json    # 일별 신규 법안
│   ├── 2026-01-20_bills.json
│   └── ...
├── bills_master.json            # 전체 법안 마스터 (누적)
└── cache/
    └── embedding_cache.json     # 임베딩 캐시 (재사용)
```

---

### 2.2 동적 임계값 시스템

#### 2.2.1 부처 프로필

```python
MINISTRY_PROFILES = {
    # 범용형 (감지율 50%+) → 높은 임계값
    "행정안전부": {"threshold": 0.52, "type": "범용형"},
    "보건복지부": {"threshold": 0.50, "type": "범용형"},
    "법무부": {"threshold": 0.50, "type": "범용형"},

    # 중간형 (감지율 20~50%) → 기본 임계값
    "산업통상부": {"threshold": 0.45, "type": "중간형"},
    "국방부": {"threshold": 0.45, "type": "중간형"},
    "교육부": {"threshold": 0.45, "type": "중간형"},

    # 특화형 (감지율 20% 미만) → 낮은 임계값
    "국토교통부": {"threshold": 0.42, "type": "특화형"},
    "문화체육관광부": {"threshold": 0.42, "type": "특화형"},
    "외교부": {"threshold": 0.42, "type": "특화형"},
}
```

#### 2.2.2 설정 파일 (`config/ministry_config.yaml`)

```yaml
ministries:
  산업통상부:
    threshold: 0.45
    type: 중간형
    keywords:
      - 에너지
      - 탄소
      - 산업
      - 통상
      - 수출입
    keyword_bonus: 0.03

  국토교통부:
    threshold: 0.42
    type: 특화형
    keywords:
      - 건설
      - 도로
      - 철도
      - 주택
    keyword_bonus: 0.02
```

---

### 2.3 관심 키워드 가산점 로직

#### 2.3.1 스코어링 공식

```
최종 스코어 = 기본 유사도(CosineSim) + 키워드 가산점

키워드 가산점 = Σ (매칭 키워드 수 × keyword_bonus)
              (최대 0.05 cap)
```

#### 2.3.2 예시

| 법안 | 기본 유사도 | 키워드 매칭 | 가산점 | 최종 스코어 | 결과 |
|------|-------------|-------------|--------|-------------|------|
| 탄소중립기본법 | 0.441 | "탄소" ✓ | +0.03 | **0.471** | ✅ 감지 |
| 일반 법안 A | 0.450 | 없음 | +0.00 | 0.450 | ✅ 감지 |
| 일반 법안 B | 0.430 | 없음 | +0.00 | 0.430 | ❌ 미감지 |

#### 2.3.3 구현 (`src/scorer_v2.py`)

```python
class ScorerV2:
    def __init__(self, ministry_config: dict):
        self.threshold = ministry_config["threshold"]
        self.keywords = ministry_config.get("keywords", [])
        self.keyword_bonus = ministry_config.get("keyword_bonus", 0.02)
        self.max_bonus = 0.05

    def calculate_keyword_bonus(self, bill: dict) -> float:
        """제목에서 키워드 매칭하여 가산점 계산"""
        title = bill.get("bill_name", "")
        matched = sum(1 for kw in self.keywords if kw in title)
        bonus = min(matched * self.keyword_bonus, self.max_bonus)
        return bonus

    def score_bill(self, bill: dict, bill_vector, rr_vector) -> dict:
        """최종 스코어 = 유사도 + 키워드 가산점"""
        base_score = cosine_similarity(bill_vector, rr_vector)
        keyword_bonus = self.calculate_keyword_bonus(bill)
        final_score = base_score + keyword_bonus

        return {
            "base_score": base_score,
            "keyword_bonus": keyword_bonus,
            "final_score": final_score,
            "is_relevant": final_score >= self.threshold,
        }
```

---

### 2.4 Teams 알림 시스템

#### 2.4.1 웹훅 메시지 포맷

```json
{
  "@type": "MessageCard",
  "themeColor": "FF5733",
  "title": "🚨 Cross-Domain Radar 일일 알림",
  "summary": "2026-01-19 신규 법안 감지",
  "sections": [
    {
      "facts": [
        {"name": "스캔 일자", "value": "2026-01-19"},
        {"name": "신규 법안", "value": "15건"},
        {"name": "감지 법안", "value": "3건"},
        {"name": "HIGH", "value": "1건"},
        {"name": "MEDIUM", "value": "2건"}
      ]
    },
    {
      "title": "감지된 법안",
      "text": "1. [HIGH] 해상풍력 보급 촉진법 (0.66)\n2. [MEDIUM] 반려동물산업법 (0.58)\n3. [MEDIUM] 에너지효율법 (0.55)"
    }
  ],
  "potentialAction": [
    {
      "@type": "OpenUri",
      "name": "상세 리포트 보기",
      "targets": [{"os": "default", "uri": "file:///path/to/report.html"}]
    }
  ]
}
```

#### 2.4.2 알림 조건

| 조건 | 알림 | 비고 |
|------|------|------|
| HIGH 1건 이상 | 즉시 알림 | 긴급 검토 필요 |
| MEDIUM 3건 이상 | 일일 요약 | 정기 검토 |
| 신규 법안 0건 | 알림 없음 | 주말/공휴일 |
| 에러 발생 | 에러 알림 | 파이프라인 장애 |

---

### 2.5 스케줄러 설정

#### 2.5.1 macOS launchd (권장)

```xml
<!-- ~/Library/LaunchAgents/com.popcorn.radar.plist -->
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "...">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.popcorn.radar</string>

    <key>ProgramArguments</key>
    <array>
        <string>/Users/sokchokim/sokcho_code/MLAF/projects/project-popcorn/.venv/bin/python</string>
        <string>/Users/sokchokim/sokcho_code/MLAF/projects/project-popcorn/run_daily.py</string>
    </array>

    <key>StartCalendarInterval</key>
    <dict>
        <key>Hour</key>
        <integer>9</integer>
        <key>Minute</key>
        <integer>0</integer>
    </dict>

    <key>StandardOutPath</key>
    <string>/Users/sokchokim/sokcho_code/MLAF/projects/project-popcorn/logs/daily.log</string>

    <key>StandardErrorPath</key>
    <string>/Users/sokchokim/sokcho_code/MLAF/projects/project-popcorn/logs/daily_error.log</string>

    <key>KeepAlive</key>
    <false/>
</dict>
</plist>
```

#### 2.5.2 실행 주기

| 시간 | 작업 | 비고 |
|------|------|------|
| 09:00 | 일배치 실행 | 전일 제안 법안 수집 |
| 09:05 | 임베딩 + 스코어링 | 캐시 활용 (~1분) |
| 09:06 | Teams 알림 발송 | HIGH/MEDIUM 있을 때 |
| 09:07 | 일일 리포트 생성 | HTML + JSON |

---

## 3. 구현 계획

### Phase 3-1: 수집 파이프라인 (Day 1)

| 모듈 | 파일 | 기능 |
|------|------|------|
| 일일 수집 | `src/ingest_daily.py` | 국회 API 신규 법안 수집 |
| 마스터 병합 | `src/merge_master.py` | 누적 데이터 관리 |

### Phase 3-2: 동적 스코어링 (Day 2)

| 모듈 | 파일 | 기능 |
|------|------|------|
| 부처 설정 | `config/ministry_config.yaml` | 동적 임계값 + 키워드 |
| 스코어러 v2 | `src/scorer_v2.py` | 키워드 가산점 로직 |

### Phase 3-3: 알림 시스템 (Day 3)

| 모듈 | 파일 | 기능 |
|------|------|------|
| Teams 알림 | `src/notifier.py` | 웹훅 메시지 발송 |
| 일일 파이프라인 | `run_daily.py` | 전체 오케스트레이션 |

### Phase 3-4: 스케줄러 (Day 4)

| 작업 | 내용 |
|------|------|
| launchd 설정 | plist 파일 생성 및 등록 |
| 로그 관리 | 로그 로테이션 설정 |
| 모니터링 | 헬스체크 스크립트 |

---

## 4. 파일 구조 (예정)

```
projects/project-popcorn/
├── config/
│   ├── ministry_config.yaml      # [NEW] 부처별 설정
│   └── teams_webhook.yaml        # [NEW] Teams 웹훅 URL
├── src/
│   ├── ingest_daily.py           # [NEW] 일일 수집
│   ├── scorer_v2.py              # [NEW] 키워드 가산점
│   ├── notifier.py               # [NEW] Teams 알림
│   ├── pipeline.py               # [UPDATE] 동적 임계값 적용
│   └── ...
├── run_daily.py                  # [NEW] 일배치 진입점
├── logs/
│   ├── daily.log                 # 일일 로그
│   └── daily_error.log           # 에러 로그
└── data/
    ├── daily/                    # 일별 수집 데이터
    └── bills_master.json         # 누적 마스터
```

---

## 5. 피드백 루프

```
┌─────────────────────────────────────────────────────────────┐
│                     피드백 루프                              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│   일배치 ──▶ Teams 알림 ──▶ 도메인 전문가 검토              │
│      │                            │                          │
│      │                            ▼                          │
│      │                     피드백 수집                       │
│      │                     - "이건 관련 없음"                │
│      │                     - "이건 놓쳤음"                   │
│      │                            │                          │
│      │                            ▼                          │
│      │                     임계값/키워드 조정                │
│      │                            │                          │
│      └────────────────────────────┘                          │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**피드백 반영 방법:**
1. Teams에서 "관련 없음" 피드백 → 해당 부처 임계값 상향
2. "놓친 법안" 리포트 → 키워드 추가 또는 임계값 하향
3. 주간 단위로 config 업데이트

---

## 6. 로컬 PC 서버 고려사항

### 6.1 장점
- 비용 없음 (클라우드 대비)
- 간단한 설정
- 빠른 구축

### 6.2 주의사항
| 항목 | 대응 |
|------|------|
| 슬립 모드 | `caffeinate` 또는 시스템 설정 |
| 재부팅 | launchd 자동 재시작 |
| 네트워크 끊김 | 재시도 로직 + 에러 알림 |
| 로그 관리 | logrotate 설정 |

### 6.3 슬립 방지 설정

```bash
# 시스템 환경설정 > 에너지 절약
# - "디스플레이가 꺼져 있을 때 자동으로 잠자기 방지" 체크

# 또는 pmset 명령어
sudo pmset -c sleep 0          # 전원 연결 시 슬립 비활성화
sudo pmset -c disksleep 0      # 디스크 슬립 비활성화
```

---

## 7. 일정 요약

| Phase | 작업 | 예상 |
|-------|------|------|
| 3-1 | 수집 파이프라인 | Day 1 |
| 3-2 | 동적 스코어링 + 키워드 | Day 2 |
| 3-3 | Teams 알림 | Day 3 |
| 3-4 | 스케줄러 + 테스트 | Day 4 |
| - | **운영 시작** | **Day 5** |

---

## 8. 다음 액션

1. **즉시**: 이 설계 검토 및 승인
2. **승인 후**: Phase 3-1 수집 파이프라인 구현 시작
3. **병렬**: Teams 웹훅 URL 확보

---

*설계 검토 후 구현 시작합니다.*
